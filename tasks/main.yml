---

- name: load os specific vars
  include_vars: "{{ ansible_distribution }}.yml"
  tags: always

- import_tasks: pkg.yml

- import_tasks: config.yml

- import_tasks: service.yml

- meta: flush_handlers

- import_tasks: fw.yml

- import_tasks: remove_other_mailers.yml